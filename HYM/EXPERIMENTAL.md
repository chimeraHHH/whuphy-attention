# Experimental Branch

This directory is retained as an experimental branch.

- Not part of the unified mainline training path.
- Paths and scripts may contain machine-specific assumptions.
- For production/reproducible workflow, use `mainline/` with `WLY/` + `Matformer-1/`.
